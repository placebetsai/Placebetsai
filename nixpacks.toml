[phases.setup]
nixPkgs = ["python311", "nodejs_20"]

[phases.install]
cmds = ["npm install", "pip3 install tiktok-uploader --quiet --break-system-packages 2>/dev/null || true"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
